apiVersion: v1
kind: Service
metadata:
  name: postgres-clusterip
spec:
  type: ClusterIP
  ports:
  - port: 5432
    protocol: TCP # если не указывать, то он будет по умолчанию
  selector:
    app: postgres
